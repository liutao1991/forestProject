<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhou.forestProject.dao.IUserDao">
    <insert id="addUser" parameterType="sysUser">
        insert into sys_user(role_id, user_name, user_pwd, user_true_name) values(
        #{roleId},#{userName},#{userPwd},#{userTrueName}
        );
    </insert>

</mapper>